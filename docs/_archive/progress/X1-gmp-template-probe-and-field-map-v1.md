# X1 GMP 模板正文探针与字段映射草案（第一版）

## 目标
在 `gmp_workshop` 已进入 `template-bound-ready` 的基础上，继续向“正式模板填充”推进一小步：

1. 读取真实 `GMP车间检测报告模板.docx` 正文文本线索
2. 对比已确认的统一模板对象（传递窗、层流罩、洁净功能用房、实验室）
3. 归纳共性字段、对象特有字段、后续填充落点方向

---

## 方法
本轮不直接做正式 XML 回填，而是先做 **模板正文探针**：

- 模板根目录：`/Users/fuwuqi/公司资料/检测部/检测报告模板`
- 读取文件：
  - `制药工业/GMP车间检测报告模板.docx`
  - `制药工业/传递窗检测报告模板.docx`
  - `制药工业/层流罩检测报告模板.docx`
  - `医院洁净部/洁净功能用房检测报告模板.docx`
  - `生物安全/实验室检测报告模板.docx`
- 方法：解压 `.docx`，读取 `word/document.xml` 中前段 `w:t` 文本
- 目标：先识别模板正文中明显的标题、概述、基础信息区、检测依据区、结论区

---

## GMP 模板正文探针结果

文件：
`/Users/fuwuqi/公司资料/检测部/检测报告模板/制药工业/GMP车间检测报告模板.docx`

### 识别到的关键文本线索
- `检测报告 / TEST REPORT`
- `报告编号：`
- `项目名称：`
- `检测区域：`
- `检测类别：`
- `委托单位：`
- `项目地址`
- `联系方式`
- `项目概述`
- `本次委托检测项目为，项目地点位于，本次检测区域。`
- `受检区域`
- `检测状态`
- `空态 / 静态 / 动态`
- `气象条件`

### 当前判断
GMP 模板目前表现为 **空间类统一模板**，结构上更接近：
- `洁净功能用房检测报告模板.docx`
- `实验室检测报告模板.docx`

而不是设备类模板（如传递窗、层流罩）。

---

## 与其他模板的结构对比

## 一、统一空间类模板的共同字段
在以下模板中都明显存在：
- GMP车间
- 洁净功能用房
- 实验室

### 共同字段
1. `报告编号`
2. `委托单位`
3. `项目地址`
4. `联系方式`
5. `项目名称`
6. `项目概述`
7. `受检区域`
8. `检测状态`
9. `气象条件`
10. `检测依据`
11. `判定依据`
12. `检测结论`

### 说明
这说明 X1 后续做正式模板填充时，可以先建立一层：

- **空间类基础信息通用映射层**

作为：
- `gmp_workshop`
- `clean_function_room`
- `bsl`
- `negative_pressure`
- `food_workshop`
- `electronics_workshop`

的公共填充底座。

---

## 二、设备类模板的差异

### 传递窗模板特有字段
- `检测对象`
- `样品名称`
- `生产厂家`
- `产品类型`
- `产品外尺寸`
- `产品内尺寸`
- `设备状态`

### 层流罩模板特有字段
- `检测对象`
- `样品名称`
- `生产厂家`
- `级别类型`
- `型号`
- `受控编号`
- `工作区尺寸`
- `设备状态`

### 判断
设备类模板和空间类模板不是一个结构体系。
所以后续不能把 `gmp_workshop` 往设备模板思路上靠。

---

## 三、GMP 第一版字段映射草案

## 可直接映射的字段（第一优先）
| X1字段 | 模板字段/语义 |
|---|---|
| `project.report_number` | `报告编号` |
| `project.client_name` | `委托单位` |
| `project.project_address` | `项目地址` |
| `project.contact_info` | `联系方式` |
| `project.project_name` | `项目名称` |
| `project.inspection_area` | `受检区域` / `检测区域` |
| `project.detection_date` | `检测日期`（若模板存在） |
| `project.detection_state` | `空态 / 静态 / 动态` |
| `project.weather.temperature` | `气象条件-温度` |
| `project.weather.humidity` | `气象条件-湿度` |
| `project.weather.pressure` | `气象条件-大气压力` |
| `room.basis` | `检测依据` |
| `room.judgement` | `判定依据` |
| `room.summary.result_state` | `检测结论` 的结果摘要来源之一 |

---

## 需要对象特有转换的字段（第二优先）
| X1字段 | 说明 |
|---|---|
| `room.context.gmp_grade` | 目前是 GMP 业务核心，但模板正文探针中尚未直接看到显式“GMP等级”文本，后续需继续确认其在表格区还是参数结果区表达 |
| `clean_class_semantics.level_semantic_key` | 不直接填模板正文，但决定模板规则与参数策略 |
| `template_rule.template_key` | 不直接填模板正文，但决定资源命中和后续填充方式 |

---

## 总结

### 已确认
1. `gmp_workshop` 已进入 X1 主链
2. 已真人前台验收进入 `template-bound-ready`
3. `GMP车间检测报告模板.docx` 真实存在且已命中
4. GMP 模板结构属于 **空间类统一模板**，不是设备类模板
5. 可以开始建立 **空间类模板公共字段映射层**

### 当前最重要的架构判断
后续正式模板填充不应该一对象一对象完全散写，而应该分两层：

#### 第一层：空间类公共字段层
- 报告编号
- 委托单位
- 项目地址
- 联系方式
- 项目名称
- 项目概述
- 受检区域
- 检测状态
- 气象条件
- 检测依据
- 判定依据
- 检测结论

#### 第二层：对象特有上下文层
- GMP 等级
- 手术室分支
- 洁净功能用房子房间
- BSL 等级
- 动物房环境链
- 负压病房压差链

---

## 验收
本轮不是“正式模板填充完成”，而是完成以下验收：

- [x] 已读取真实 GMP 模板正文线索
- [x] 已与传递窗/层流罩/洁净功能用房/实验室模板做结构对比
- [x] 已形成 GMP 第一版字段映射草案
- [x] 已明确 GMP 应归入“空间类统一模板”路线，而不是设备类路线

---

## 下一步建议
优先做：

1. 把 **空间类公共字段映射层** 抽成一份 X1 文档/代码草案
2. 再继续推进 `negative_pressure`

这样后面接：
- `negative_pressure`
- `food_workshop`
- `electronics_workshop`

都会更顺，不会每个对象都重新摸一遍模板基础字段。
