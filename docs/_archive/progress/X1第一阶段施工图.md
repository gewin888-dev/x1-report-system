# X1 第一阶段施工图

## 一、当前动作

已正式启动 X 版本，目录：
- `/Users/fuwuqi/检测报告生成系统_X1`

当前已完成基础骨架创建，并从 T1 复制了第一批可继承资产：

### 已复制文件
1. `templates/record_index.html`
2. `static/record.css`
3. `static/standards_db.js`
4. `static/standards_ranges.json`
5. `template_config.json`

### 已创建目录
- `static/`
- `templates/`
- `records/`
- `reports/`
- `logs/`
- `adapters/`
- `docs/`

---

## 二、X1 第一阶段目标

第一阶段不追求全对象覆盖，只做：

1. 建立 X1 独立骨架
2. 建立独立 canonical model 设计
3. 建立独立暂存/恢复/导出主链接口骨架
4. 先用 `pass_box` 做第一批样板对象

---

## 三、第一阶段硬性顺序

必须按以下顺序推进：

1. **先建模型**
2. **再接页面**
3. **再接暂存**
4. **再接恢复**
5. **最后接导出**

禁止反过来继续走“T里先补页面、后补模型”的老路。

---

## 四、第一阶段模块拆分建议

### 前端模块
建议在 `static/` 下逐步新增：

1. `x-model.js`
   - ProjectModel / RoomModel / ParamModel / DraftModel 定义

2. `x-range.js`
   - 统一解析 `database/manual` 两类来源

3. `x-render.js`
   - 页面渲染逻辑
   - 根据对象类型渲染参数区

4. `x-collect.js`
   - 页面 → canonical model

5. `x-validate.js`
   - 业务校验逻辑

6. `x-draft.js`
   - 暂存、草稿读取、恢复

7. `x-export.js`
   - canonical model → export payload

### 后端模块
建议先在 `app_x1.py` 中建立独立接口骨架：

- `/api/x/save_draft`
- `/api/x/load_draft`
- `/api/x/submit_export`
- `/api/x/list_drafts`
- `/api/x/list_reports`

---

## 五、样板对象优先级

第一批样板对象：
1. `pass_box`
2. `laminar_hood`
3. `operating_room`
4. `clean_function_room`
5. `bsl`

### 第一优先样板：`pass_box`
原因：
- 已有真实测试基础
- 设备类，结构相对收敛
- 等级、判定、暂存、恢复、导出链都已经在 T 中暴露过典型问题
- 适合先做 X 的统一链样板

---

## 六、第一阶段验收标准

### 必须通过的四条真实链路
对 `pass_box` 样板对象，至少验证：

1. 新建录入
2. 暂存成功
3. 编辑恢复成功
4. 导出成功

### 结构验收
- 不引入 `ready/source/live/saved` 旧体系
- `dataset` 不承担持久业务真相
- 参数来源只允许 `database/manual`
- 页面、暂存、恢复、导出统一吃同一 canonical model

---

## 七、当前判断

X1 已正式启动，但目前还只是**基础骨架阶段**，还未进入 canonical model 与独立主链实现阶段。

下一步应立即进入：

### 下一步主任务
1. 新建 `docs/X1-canonical-model.md`
2. 定义 `ProjectModel / RoomModel / ParamModel / DraftModel`
3. 新建 `static/x-model.js`
4. 从 `pass_box` 开始建立 X1 的第一条独立录入主链

---

_创建时间：2026-04-14 17:43 GMT+8_
_用途：作为 X1 第一阶段施工记录与执行起点_
