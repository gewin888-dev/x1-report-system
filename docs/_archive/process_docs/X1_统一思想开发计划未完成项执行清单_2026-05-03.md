# X1_统一思想开发计划未完成项执行清单_2026-05-03.md

更新时间：2026-05-03 10:15
依据：
- `~/Desktop/统一思想/X1项目统一思想文档_2026-05-02.md`
- `~/Desktop/统一思想/X1_收口治理路线图_P0-P2_V1.1_2026-05-02.md`
- `~/Desktop/统一思想/X1_Canonical_Payload标准规范_V1.1_2026-05-02.md`
- `~/Desktop/统一思想/X1_接口兼容映射表_V1.1_2026-05-02.md`
- `~/Desktop/统一思想/X1_对象模板映射矩阵_V1.0_2026-05-02.md`
- `~/Desktop/统一思想/X1_模板资源清单与状态表_V1.0_2026-05-02.md`
- `~/Desktop/统一思想/X3_全量业务对象主台账（51项）.md`

> 目的：把“统一思想文件夹里的开发计划还没有完成”这件事显式化，转成可持续执行的状态清单。

---

## 一、总判断

当前不再按“发现一个 bug 修一个”的口径推进，改为按统一思想里的 **P0 → P1 → P2** 主线持续收口。

### 2026-05-09 阶段判断更新
- **P0：对象级可信基线、导出填充主链、关键判定链、服务健康恢复已完成本轮收口**
- **P1：前台真验、外部链路、文档摘要层同步仍需持续维护，但已不再属于“主链未通”问题**
- **P2：发布前治理项仍大部分待做**

### 当前阶段判断
- **P0：可信基线已基本收口，断网专项主链已打通；尾项主要剩前台真验 / 更高层外部链路**
- **P1：工程结构已明显收口，但前台显示链 / 模板业务变体治理 / 页面验收证据仍未全部完成**
- **P2：大部分尚未完成**

---

## 二、P0 止血项执行状态

| 编号 | 事项 | 当前状态 | 判断依据 | 下一步动作 |
|---|---|---|---|---|
| P0-1 | 输入标准化与兼容映射收口 | **进行中** | `app_x1.py` 已接 `payload_normalizer.normalize_project_payload`；但测试脚本仍有旧口径混用，兼容追溯字段 `trace_id/record_version/normalized_at` 未见全面落地 | 核查 `payload_normalizer.py` 全实现；补兼容追溯字段；清理旧脚本口径 |
| P0-2 | 导出对象串型问题 | **基本完成，待复核** | 未见当前代码静默 `type_id -> pass_box` 兜底主链；但 `app_x1.py` 某些 fallback / `pass_box` 兼容痕迹仍多，需做跨对象抽样复核 | 做 5 跨领域对象抽样，核 `export_type == room.type_id` |
| P0-3 | 导出前关键字段守门校验 | **已接入，待覆盖复核** | `validate_normalized_project(project)` 已在 `/api/x/build_export`、`/api/x/submit_export` 等入口使用 | 补缺字段实测，确认 12 条守门规则是否齐全 |
| P0-4 | 重建可信测试基线 | **已完成，并已在 2026-05-09 得到新一轮现场验证加固** | 扩展13项在线深测已达 `13/13`；51对象统一深测已达 `51/51 chain_ok，51/51 deep_ok`；全量在线主链真实记录已达 `48/48`；测试器 docx 校验噪声已修正；全参数 Batch2~Batch5 累计 `24/24`；边界 Batch2~Batch4b 已完成关键对象边界核实与纠偏；异常样本 Batch1~Batch2b 已完成代表批次覆盖；`docs/X1_对象级测试闭环矩阵_2026-05-03.md` 已确认 **14/14 已覆盖对象形成五层闭环**；断网 O1~O5 浏览器级真测主链已打通；且 2026-05-09 现场新增验证：`test_judgement_engine_regression.py` → `14/14 PASS`、`test_operating_room_content_regression.py` → `77/77 PASS`、`test_animal_room_content_regression.py` → `76/76 PASS`、`test_device_content_regression.py` → `49/49 PASS`、`status_x1_daemon.sh` → 服务健康 PASS | 后续主线从“补对象级可信基线”切换为“高层验收专项 + 发布前治理” |

---

## 三、P1 结构收口项执行状态

| 编号 | 事项 | 当前状态 | 判断依据 | 下一步动作 |
|---|---|---|---|---|
| P1-1 | 正式模型文档与映射文档 | **基本完成** | 桌面统一思想目录 5 份核心文档已齐 | 后续仅做状态同步和版本维护 |
| P1-2 | 模板规则层收口 | **进行中** | 高风险对象模板规则多数已能命中；`operating_room` 辅房样本已纠正测试构造并回到 aux 分支；`pass_box` / `animal_room` / `ivc` 等对象在异常样本纠偏后已进入真实判定链；但 `pass_box` 在 51 变体口径下仍仅有 `default` 规则，`B1/B2`、`B3` 分支尚未形成独立模板规则闭环 | 清理/确认 `pass_box` 业务变体是否需要独立模板 key；继续消灭资源待确认项 |
| P1-3 | 前台显示链收口 | **进行中（`operating_room` / `animal_room` / `clean_function_room` / `negative_pressure` / `bsl` / `gmp_workshop` / `food_workshop` / `veterinary_gmp_workshop` / `laminar_hood` 已收口）** | 已通过 `/api/list` 收口第一视图门槛：隐藏 `全变体测试-*` / 边界测试自动草稿，空白/半空白草稿卡片已清零（`blank_like_drafts = 0`）；并已从代码链/数据链确认“已生成记录回编辑”场景下 `result_state/judgement_reason` 可进入房间摘要；管理页飞书状态页已通过浏览器级探针 `test_admin_feishu_status_probe.js` 实测，目标样本 `feishu_fail_probe_20260503_151802` 已显示 `报告飞书✅；记录飞书✅`，并生成 `reports_x1/admin_feishu_status_20260503072954.json/.png` 证据；前台录入页已先后通过浏览器级探针 `test_frontend_visual_probe.js`、`test_frontend_visual_probe_electronics.js`、`test_frontend_visual_probe_operating_room.js`、`test_frontend_visual_probe_clean_function_room.js`、`test_frontend_visual_probe_negative_pressure.js`、`test_frontend_visual_probe_bsl.js`、`test_frontend_visual_probe_gmp_workshop.js`、`test_frontend_visual_probe_food_workshop.js`、`test_frontend_visual_probe_veterinary_gmp_workshop.js`、`test_frontend_visual_probe_laminar_hood.js` 实测，`pass_box`、`electronics_workshop`、`operating_room`、`clean_function_room`、`negative_pressure`、`bsl`、`gmp_workshop`、`food_workshop`、`veterinary_gmp_workshop`、`laminar_hood` 十类对象在草稿加载后，`projectInfoSummary / room-summary / room-card dataset` 均能呈现 `result_state / input_result_state / judgement_engine / judgement_reason / abnormal_items`；其中 `gmp_workshop` 复测样本 `reports_x1/frontend_restore_gmp_workshop_202605031249_gmp_workshop.json/.png` 已确认 `wind_speed / pressure / noise` 的值与结果均已真实恢复；`food_workshop` 复测样本 `reports_x1/frontend_restore_food_workshop_202605031333_food_workshop.json/.png` 已确认历史 `Ⅲ级` 记录可自动命中前台等级按钮、参数区已建立，且 `airchange / pressure / noise` 的值与结果均已真实回填，页面不再卡在“请先选择洁净等级”；`veterinary_gmp_workshop` 样本 `reports_x1/frontend_restore_veterinary_gmp_workshop_202605031338_veterinary_gmp_workshop.json/.png` 已确认 A 级历史记录在回编辑场景下 `projectInfoSummary / room-summary / dataset / 参数区` 全链路恢复正常，`wind_speed / pressure / noise` 的值与结果均已真实回填；`laminar_hood` 样本 `reports_x1/frontend_restore_laminar_hood_202605031350_laminar_hood.json/.png` 已确认层流罩对象在回编辑场景下 `projectInfoSummary / room-summary / dataset` 与关键结果项恢复正常；同时已新建正式摘要页 `docs/X1_前台回填链验收摘要_2026-05-03.md`，把当前已收口对象的浏览器级正向证据收成一页，便于后续汇报与续会话接力 | 下一步继续扩到 `pass_box` 等高风险对象 |
| P1-4 | 草稿/恢复/导出链统一 | **进行中** | normalize 已接入 save_draft/submit_export 主链；追溯字段 `trace_id/record_version/normalized_at` 已落地；已用 `operating_room` 辅房与 `electronics_workshop` 样本验证“保存→恢复→导出”不串型 | 继续扩展高风险对象一致性抽检，并补前台恢复显示验证 |

---

## 四、P2 发布前治理项执行状态

| 编号 | 事项 | 当前状态 | 判断依据 | 下一步动作 |
|---|---|---|---|---|
| P2-1 | 操作日志真实实现 | **未完成** | 尚未见正式操作日志方案落地证据 | 设计并落地结构化操作日志 |
| P2-2 | 分页与性能治理 | **未完成** | 暂未见正式分页/性能验收闭环 | 补列表分页与性能检查 |
| P2-3 | 飞书上传鲁棒性 | **进行中（成功态/失败态落账 + 页面可见 + 重传闭环已实证）** | 已确认配置与上传链存在；`feishu_utils.py` 已具备 token / 年份目录 / 上传重试基础；`app_x1.py` 已修正为无论飞书上传成功失败，都会将 `feishu.report/export` 结果（含 `success/error`）落账到导出 JSON；`test_feishu_probe.py` 已生成 `reports_x1/feishu_probe_20260503_151452.json(.md)`，验证成功态 `report_success=True`、`export_success=True`；`test_feishu_fail_probe.py` 已生成 `reports_x1/feishu_fail_probe_20260503_151802.json(.md)`，验证受控失败态 `report_success=False`、`export_success=False` 且错误信息稳定落账；管理页 `admin/api/records` 已新增 `feishu_report_status / feishu_export_status / feishu_report_error / feishu_export_error` 字段，`templates/admin.html` 已可直接显示“报告飞书✅/❌、记录飞书✅/❌”；并已新增 `POST /admin/api/records/<record_id>/retry_feishu` 最小重传入口，已对失败样本 `X1EXPORT_20260503151802` 实测重传成功，状态从 failed 回正到 success；随后 `test_admin_feishu_status_probe.js` 又补了浏览器级页面探针，`feishu_fail_probe_20260503_151802` 已在管理页实测显示 `报告飞书✅；记录飞书✅`，并生成 `reports_x1/admin_feishu_status_20260503072954.json/.png` 证据 | 下一步补更真实的外部失败场景（token/网络异常）证据，或视交付需要再补更细的页面提示 |
| P2-4 | 清理与运维治理 | **未完成** | 尚无统一运维治理结果单 | 梳理 trash/导出缓存/日志保留策略 |
| P2-5 | 剩余业务缺口补齐 | **进行中** | `laminar_hood` 代码侧已有判定函数，且本轮已补齐浏览器级对象真验与关键参数边界真验（`avg_speed / hepa_leak / airflow_pattern / speed_uniformity`）；其余残余专项未全清 | 继续补 `pass_box` / 边界专项收口 |

---

## 五、当前已经发现并进入修复中的真实问题

### 1. 深测判定摘要链缺失
- 现象：扩展对象在线主链能通，但 `judgement_engine / judgement_reason` 为空
- 当前处理：
  - 已在 `app_x1.py` 增加未命中判定时的明确兜底摘要
  - 已在 `test_x1_all_variants.py` 补“最小可判定参数”，让扩展对象尽量进入真实判定链
- 当前状态：**已开始修复并验证部分对象生效**

### 2. 测试样本口径混用，污染结论
- 现象：旧脚本有的仍用历史字段 / 弱样本 / 非 Canonical 结构
- 当前状态：**进行中**
- 处置方向：用统一记录器逐步替换旧脚本的决策权

### 3. 51 对象高层验收体系未完成
- 现象：对象级五层闭环已完成，且 **断网 O1~O5 浏览器级真测主链已打通**；但 **前台显示链 / 页面回填 / 外部链路** 这些更高层专项仍未全部形成最终验收证据
- 当前状态：**进行中**
- 处置方向：把主线从“对象级可信基线”切到“前台真验 + 外部链路验收专项”

---

## 六、从现在开始的执行顺序

### 第一优先级：让结果可信（P0 收口）
1. 完成扩展 13 项在线深测重跑 ✅（已达 13/13）
2. 完成 51 对象主链/深测统一记录 ✅（已达 51/51 chain_ok，51/51 deep_ok）
3. 完成对象级五层闭环 ✅（已达 14/14）
4. 补 `test_fullchain_recorded.py` / 全参数 / 边界 / 异常样本代表批次 ✅
5. 当前 P0 剩余尾项：**前台真验 / 外部链路专项**

### 第二优先级：让结构闭合（P1 收口）
6. 做前台显示链专项
7. 做草稿→恢复→导出一致性专项
8. 做模板资源待确认项清理 / `pass_box` 业务变体规则核实

### 当前新增最高优先事项（13:42 后口径）
9. **把测试主线从对象级闭环切到高层验收专项**
   - 断网 O1~O5
   - 前台显示/页面回填
   - 飞书/外部链路
   - 真人链路证据

### 第三优先级：让系统可交付（P2 收口）
10. 飞书一致性策略
11. 操作日志 / 分页 / 清理治理
12. 剩余专项与运维治理收口

---

## 七、今日执行口径

今天后续工作，不再按“单点修复”记账，而按以下口径记账：

- 属于 P0 / P1 / P2 的哪一项
- 是在补可信基线，还是补结构收口，还是补发布治理
- 每次改动后必须补：
  1. 代码证据
  2. 测试证据
  3. 台账同步
  4. 当前剩余未完成项
