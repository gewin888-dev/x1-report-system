# X1 断网专项推进记录（2026-05-03 14:49 后）

## 当前结论

### 已确认的代码级离线能力骨架
基于 `static/record.js` 与 `test_offline_capability_probe.js`，当前已确认前端具备以下离线能力骨架：

1. `isOnline` 分支存在
2. `saveToLocal()` 已实现
3. `buildOfflineTask()` 已实现
4. 本地离线记录支持 `LOCAL_` 标识回查
5. 已存在离线暂存提示：`已保存到本地`
6. 已存在网络异常回退提示：`网络异常,已保存到本地`
7. 草稿列表渲染链存在：`draftRecordsList`
8. 本地队列动作已覆盖：
   - `save_draft`
   - `submit_only`
   - `submit_and_generate`
   - `submit_and_export`
9. 已存在 `getLocalTaskByRecordId()` 与 `LOCAL_` 编辑加载路径

### 已有静态探针结果
- 脚本：`test_offline_capability_probe.js`
- 既有结论：**12/12 命中**

### 新增浏览器级真测进展（Playwright）
- 已补环境：
  - `package.json`
  - `playwright`
  - `chromium`
- 已将 `test_offline_browser_flow.js` 从骨架补成可执行版
- 当前已真实打通并记录：
  1. **O1 在线录入中断网后暂存：通过**
     - 结果：出现本地保存提示，IndexedDB 内生成 `save_draft` 待同步任务
  2. **O2 纯断网新建：通过**
     - 结果：断网状态下可完成录入与本地落盘，页面存在可见证据，IndexedDB 中存在本地任务
  3. **O3 断网草稿列表：通过**
     - 结果：离线状态下草稿页可见本地记录，能看到项目名/客户名/待同步状态
  4. **O4 断网摘要回填：通过（主链）**
     - 已确认旧问题来自前端 `normalizeEditableRecordData()` 未兼容 `domain_id -> domain`
     - 修复后离线记录重新加载时：
       - `roomCount` 已从 `0` 恢复到 `1`
       - `type_id=pass_box` 卡片已恢复
       - `dataset.resultState / judgementReason / abnormalItems` 已恢复
     - 当前剩余细点：`dataset.passBoxResultState` 仍会被 live 链覆盖为空，属于专项字段残点，不影响主摘要链结论
  5. **O5 恢复联网继续提交/导出：通过（主链）**
     - 已用满足前置条件的最小离线 `submit_and_export` 样本复测
     - 结果：
       - 离线队列任务成功生成（`status=pending`）
       - 恢复联网后进入同步（`status=processing`）
       - 同步完成后本地队列清空（`afterSync=[]`）
       - 页面出现提示：`✅ 已同步并导出1条离线记录`

## 当前总体判断
断网专项当前不应再表述为“仍在收口”或“仅部分成立”，更准确的说法是：

> **O1~O5 浏览器级断网真测主链已全部拿到正向证据，断网专项主链已打通。**

## 当前剩余细点
1. `pass_box` 的 `dataset.passBoxResultState` 在离线摘要回填场景下仍会被 live 链覆盖为空
   - 当前性质：专项字段残点
   - 当前影响：不影响 O4 主摘要链恢复成立，不影响断网主链通过结论

## 当前建议
1. 将断网专项状态升级为“主链通过”
2. 在总台账中把 O1~O5 标记为已打通
3. 若继续追求更严验收，再单独补 `passBoxResultState` 这个专项残点
