# X1 clean_function_room 接力卡

## 当前真实状态
- 对象：`clean_function_room / ICU病房`
- 当前阶段：已进入真实 `.filled.docx` 链路，不再是仅 `template-bound-ready` 的空转阶段。
- 最新第二轮真实导出：`X1EXPORT_20260416093808`
- 产物：
  - `reports_x1/X1EXPORT_20260416093808.json`
  - `reports_x1/X1EXPORT_20260416093808.docx`
  - `reports_x1/X1EXPORT_20260416093808.bound.docx`
  - `reports_x1/X1EXPORT_20260416093808.filled.docx`

## 本轮改了什么
- 采用“最小可验 ICU 样本”直接走 `POST /api/x/submit_export`。
- 修正了上一轮一个关键调用误区：`/api/x/submit_export` 需要提交 **project 形态** 数据，不能把历史 `export_payload/final_payload` 直接回灌进去。
- 构造了带最小参数样本的 ICU project payload，补入：
  - 基础字段：项目名称、委托单位、地址、检测区域、报告编号、检测日期
  - room.params：换气次数、静压差、温度、相对湿度、照度、噪声、沉降菌、粒子
- 结果确认：参数已经进入新的 `export_payload.room.params`，模板也成功命中 ICU 专属模板。

## 本轮验证证据
- 导出成功：`X1EXPORT_20260416093808`
- `export_payload.template_rule.template_key = hospital/clean_function_room/icu`
- `export_payload.template_resource.template_found = true`
- `export_payload.room.params` 已包含：
  - `air_change_rate=18`
  - `static_pressure_diff=12`
  - `temperature=22.6`
  - `relative_humidity=54`
  - `illumination=385`
  - `noise=52`
  - `settling_bacteria=1`
  - `cleanliness_05um=320000`
  - `cleanliness_5um=2100`
- `word/document.xml` 实检结果：
  - 已确认基础信息进入模板：项目名称、委托单位、地址、检测区域、报告编号
  - 已确认“项目概述”完整句已在 `report_context.project_context.project_overview_text` 中生成
  - 但 ICU 模板正文表格值单元格仍大量为空：
    - `换气次数` 行结果单元格为空
    - `静压差` 行结果单元格为空
    - `温度` 行结果单元格为空
    - `相对湿度` 行结果单元格为空
    - `平均照度` 行结果单元格为空
    - `噪声` 行结果单元格为空
    - `细菌浓度(沉降法)` 行结果单元格为空
    - 粒子区块中的最大值/UCL 也未落进去
- 同时发现：
  - 模板中“项目概述”当前仍显示为：`本次委托检验项目为，项目地点位于。本次检测区域ICU病房一区`
  - 说明虽然 `project_overview_text` 已正确生成，但尚未实际写入 ICU 模板承载位
  - `weather_text` 仍未落入模板，气象条件区域仍为空壳

## 当前阻塞点
- 不是数据源问题：第二轮已经证明参数值确实进入 `export_payload.room.params`。
- 真正阻塞点在 `adapters/template_fill.py`：
  - `clean_function_room` 分支目前仍主要依赖基于标签的通用替换；
  - 但 ICU 模板正文是“复杂表格承载”，当前填充策略没有把 params 精确写入目标单元格。
- 另一个并行缺口：`project_overview_text` / `weather_text` 生成了，但 ICU 模板里没有被精确落位。

## 下一步唯一动作
- 直接进入 `adapters/template_fill.py`，为 `clean_function_room / ICU病房` 增加 **面向 ICU 表格结构的定点单元格填充规则**：
  1. 先修“项目概述”与“气象条件”两处正文承载位；
  2. 再按 ICU 表格结构，把 `换气次数/静压差/温度/湿度/照度/噪声/沉降菌/粒子最大值/UCL` 逐项写入检测结果单元格；
  3. 完成后再跑第三轮真实导出验收。


## 09:45 第三轮真实验收补充
- 最新导出：`X1EXPORT_20260416094452`
- 本轮动作：在 `adapters/template_fill.py` 内补了 ICU 定点填充规则，并放宽 `clean_function_room` 参数读取逻辑，兼容 `key/code/label` 三种参数键来源。
- 新增有效结果：
  - 正文表格 `换气次数` 检测结果已写入：`18`
  - 正文表格 `静压差` 检测结果已写入：`12`
  - 正文表格 `平均照度` 检测结果已写入：`385`
  - 粒子区块已写入：`320000`、`2100`
  - 仪器表侧也出现了部分误写入现象（说明 anchor 命中过宽）
- 仍未闭合的缺口：
  - `项目概述` 仍未写入完整句
  - `气象条件` 仍未写入具体值
  - `温度` 结果 `22.6` 仍未进入正文检测结果位
  - `噪声` 结果位仍空
  - `细菌浓度(沉降法)` 结果位仍空
- 新判断：当前 ICU 定点写入已证明“可打进去”，但 anchor 仍偏粗，下一轮要从“能写进去”切到“只写对位置”。
- 下一步唯一动作：收紧 `template_fill.py` 对 ICU 的锚点规则，避免误写到仪器表，并专门补 `项目概述/气象条件/温度/噪声/沉降菌` 的正确落位。


## 09:52 第四轮真实验收补充
- 最新导出：`X1EXPORT_20260416095208`
- 本轮动作：继续收紧 ICU 锚点，并补上 `project_overview_text` 进入 fill_plan；同时把测试 payload 改成真正的 `project.weather` 结构，验证 `weather_text` 真实生成链路。
- 本轮确认闭合：
  - `项目概述` 已正确落位为完整自然句
  - `气象条件` 已正确落位为：`温度：21.8℃  湿度：35%RH  大气压力：890hPa`
  - 正文表格 `换气次数=18`、`静压差=12`、`相对湿度=54`、`平均照度=385`、`噪声=52` 已正确落位
- 当前剩余缺口：
  - `温度=22.6` 仍误写进仪器表，正文结果位仍空
  - `细菌浓度(沉降法)=1` 仍未落进正文结果位
  - 粒子区块目前只写进了最大值（320000/2100），UCL 仍为空
- 新判断：公共摘要区（项目概述/气象条件）和 ICU 正文大部分参数位已经收口，剩余问题已缩小到少数“同名锚点冲突 + 特殊行命名差异”问题。
- 下一步唯一动作：只攻 `温度` 与 `细菌浓度(沉降法)` 两个点位的专属锚点，必要时直接按 ICU 表格行号/表号定点写入，不再走宽泛文本锚点。


## 09:56 第五轮真实验收补充
- 最新导出：`X1EXPORT_20260416095635`
- 本轮动作：不再用宽泛文本锚点处理 `温度`，而是新增“按表号+行号+单元格号”直接定点写入能力，并把 ICU 正文温度行改为直接落在 Table4 / Row12 / Cell3。
- 本轮确认闭合：
  - `温度=22.6` 已正确进入 ICU 正文结果位，且不再误写入仪器表
  - `细菌浓度(沉降法)=1` 已正确进入 ICU 正文结果位
- 当前 ICU 正文已成功落位的参数：
  - 换气次数 18
  - 静压差 12
  - 温度 22.6
  - 相对湿度 54
  - 平均照度 385
  - 噪声 52
  - 细菌浓度(沉降法) 1
  - 粒子最大值 320000 / 2100
- 当前剩余缺口：
  - 浮游菌（平均浓度）仍空
  - 粒子区块 95%UCL 仍空
  - `洁净度级别（悬浮粒子浓度）` 首行的“洁净度级别/悬浮粒子数/m³/结论”布局已部分落位，但还不算最终精修完成
- 新判断：ICU 第二轮到第五轮的收口已完成核心目标——正文主参数结果位基本打通。现在已从“链路打通”阶段进入“粒子/UCL/浮游菌精修”阶段。
- 下一步唯一动作：只做 ICU 粒子区块和浮游菌的精修验收，完成后即可把方法复制到 CSSD / 透析室 / 通用洁净功能用房。


## 10:03 第六轮真实验收补充
- 最新导出：`X1EXPORT_20260416100301`
- 本轮动作：为 ICU 增补 `浮游菌（平均浓度）` 落位；同时把粒子对象补成带 `ucl_0_5um/ucl_5um` 的真实样本，并改用按表号+行号直写的方式补 `95%UCL`。
- 本轮确认闭合：
  - `浮游菌（平均浓度）=120` 已正确进入 ICU 正文结果位
  - 粒子区块 `0.5μm 95%UCL = 300000` 已落位
  - 粒子区块 `5μm 95%UCL = 1800` 已落位
  - 粒子最大值 `320000 / 2100` 仍保持正确落位
- 当前 ICU 正文结果区已完整落位的核心数据：
  - 换气次数 18
  - 静压差 12
  - 温度 22.6
  - 相对湿度 54
  - 平均照度 385
  - 噪声 52
  - 细菌浓度(沉降法) 1
  - 浮游菌（平均浓度）120
  - 粒子最大值 320000 / 2100
  - 粒子 UCL 300000 / 1800
- 当前剩余问题：
  - 粒子区块首行/说明位仍有精修空间（例如模板首行“洁净度级别/悬浮粒子数/m³/结论”布局虽可用，但还不算最终最优展示）
  - 仪器表第4行出现了 `ISO-8`、`320000`、`合格` 这类业务值串入，说明“洁净度级别（悬浮粒子数）”相关通用替换仍偏宽，后续应收紧，避免污染仪器表
- 新判断：ICU 核心业务结果已经达到“可用样板”水平；下一步从“功能打通”切换到“模板洁净化/误写清理”。
- 下一步唯一动作：收紧 `洁净度级别（悬浮粒子浓度）` 的通用替换，禁止业务值再写入仪器表；完成后即可把 ICU 方案复制到 CSSD / 透析室 / 通用洁净功能用房。


## 10:06 第七轮真实验收补充
- 最新导出：`X1EXPORT_20260416100633`
- 本轮动作：清理 ICU 仪器表误写，删除 `clean_function_room` 不再需要的粒子/洁净度通用表格替换入口，改为只保留 ICU 专属正文区定点写入。
- 本轮确认闭合：
  - 仪器表第4行已恢复正常，不再出现 `ISO-8 / 320000 / 合格` 这类业务值串入
  - ICU 正文粒子区块与其他主参数结果位保持正确不回退
- 当前 ICU 样板状态：
  - 业务主结果位完整可用
  - 公共摘要区完整可用
  - 仪器表误写已清理
- 仍可继续优化的点：
  - 粒子区块首行 `洁净度级别（悬浮粒子浓度）` 的展示可进一步精修（目前能用，但不是最优展示）
- 新判断：ICU 已从“可用样板”进一步提升到“可复制样板”。
- 下一步唯一动作：将 ICU 的结构化定点填充方法平移到 CSSD / 透析室 / 通用洁净功能用房，并逐个做真实导出验收。


## 10:15 第八轮真实验收补充（CSSD首轮）
- 最新导出：`X1EXPORT_20260416101534`
- 本轮动作：开始复制 ICU 已收口的方法到 CSSD，先基于已验证 ICU payload 生成 CSSD 首轮项目样本，并直接跑真实 `.filled.docx` 验收。
- 本轮验证结果：
  - CSSD 模板主链已跑通，未出现仪器表污染回归
  - CSSD 摘要区可用：项目名称、项目概述、受检区域、气象条件均已落位
  - CSSD 正文主参数结果位可用：换气次数 20、静压差 15、温度 21.9、相对湿度 50、平均照度 410、噪声 50、细菌浓度(沉降法) 2、浮游菌（平均浓度）110
  - CSSD 粒子区块可用：最大值 280000 / 1800，UCL 250000 / 1500
- 当前判断：ICU 方案不是只适配单模板，而是已经具备跨 `clean_function_room` 子模板的迁移能力。CSSD 首轮即通过，说明当前结构化定点填充方法具备通用性。
- 当前剩余问题：
  - 仍需继续对“透析室”“通用洁净功能用房”各跑一轮真实验收，确认不存在模板结构差异导致的个别点位偏移
- 下一步唯一动作：继续复制同一方法到“透析室”，做首轮真实 `.filled.docx` 验收。


## 10:22 第九轮真实验收补充（透析室首轮）
- 最新导出：`X1EXPORT_20260416102251`
- 本轮动作：继续把 ICU 已验证的结构化定点填充方法平移到“透析室”，直接做首轮真实 `.filled.docx` 验收。
- 本轮验证结果：
  - 透析室模板主链已跑通，未出现仪器表污染
  - 摘要区可用：项目名称、项目概述、受检区域、气象条件均正常落位
  - 正文主参数结果位可用：换气次数 18、静压差 10、温度 22.2、相对湿度 48、平均照度 360、噪声 49、细菌浓度(沉降法) 2、浮游菌（平均浓度）100
  - 粒子区块可用：最大值 260000 / 1600，UCL 230000 / 1400
- 当前判断：`clean_function_room` 已经连续完成 ICU、CSSD、透析室 3 个子模板的真实迁移验证，说明当前方法不是单模板特例，而是可重复复制的统一收口路径。
- 当前剩余问题：
  - 仅剩“通用洁净功能用房”尚未跑真实验收
- 下一步唯一动作：继续复制同一方法到“通用洁净功能用房”，完成 `clean_function_room` 这一批子模板的首轮全覆盖验收。


## 10:25 第十轮真实验收补充（通用洁净功能用房首轮）
- 最新导出：`X1EXPORT_20260416102515`
- 本轮动作：完成 `clean_function_room` 最后一个子模板“通用洁净功能用房”的首轮真实 `.filled.docx` 验收。
- 本轮验证结果：
  - 通用洁净功能用房模板主链已跑通，未出现仪器表污染
  - 摘要区可用：项目名称、项目概述、受检区域、气象条件均正常落位
  - 正文主参数结果位可用：换气次数 16、静压差 8、温度 22.0、相对湿度 52、平均照度 340、噪声 48、细菌浓度(沉降法) 2、浮游菌（平均浓度）90
  - 粒子区块可用：最大值 240000 / 1500，UCL 210000 / 1300
- 当前判断：`clean_function_room` 四个子模板（ICU / CSSD / 透析室 / 通用洁净功能用房）均已完成首轮真实导出验收，且同一套结构化定点填充方案全部通过，说明该对象已完成“首轮全覆盖打通”。
- 当前剩余问题：
  - 仍有可做的展示精修，但已不属于“是否打通”的范畴
  - 下一阶段应从“子模板逐个首轮验收”切换到“对象级总结 + 进入下一对象”
- 下一步唯一动作：整理 `clean_function_room` 的对象级收口结论，更新对象任务卡状态，并据此切换到下一个优先对象。
