# X1 兼容接口修复任务卡（2026-04-17）

1. 目标
- 修复旧客户端仍调用 `/api/submit_and_export` 与 `/api/list` 时产生的 404。
- 补一份最小兼容回归验证，确保新旧接口都能走通。
- 把本轮修复结果落成文档，避免只停留在对话里。

2. 计划
- 先在 `app_x1.py` 增加旧接口兼容路由，复用现有 `/api/x/*` 正式逻辑。
- 修正 `/api/x/meta` 的过时状态描述，使其反映实际能力。
- 新增一个最小回归脚本，使用 Flask `test_client` 直接验证保存、草稿、导出、列表的新旧接口链路。
- 形成任务卡，并回写总台账状态更新。

3. 方法
- 对 `POST /api/submit_and_export` 做兼容转发，内部复用 `api_x_submit_export()`。
- 对 `GET /api/list` 做兼容映射，内部复用 `api_x_list_exports()`。
- 在兼容响应中增加：`compat: true` 与 `compat_route`，便于后续观察旧接口是否还在被依赖。
- 使用 `temp_x1/validate_x1_compat.py` 通过 `app.test_client()` 做本地回归，避免依赖当前是否已有监听进程。

4. 结果
- `app_x1.py` 已新增：
  - `POST /api/submit_and_export`
  - `GET /api/list`
- `/api/x/meta` 已从“仅提供健康检查与元信息接口”修正为当前真实能力说明。
- 已新增最小回归脚本：`temp_x1/validate_x1_compat.py`。

5. 验收
- 代码语法已通过：`python3 -m py_compile '/Users/fuwuqi/检测报告生成系统_X1/app_x1.py'`
- 待执行兼容回归脚本：
  - `/Users/fuwuqi/检测报告生成系统_X1/temp_x1/validate_x1_compat.py`
- 回归目标：
  - `/api/x/health`
  - `/api/save`
  - `/api/x/list_drafts`
  - `/api/x/load_draft/<draft_id>`
  - `/api/x/submit_export`
  - `/api/submit_and_export`
  - `/api/x/list_exports`
  - `/api/list`

6. 总结
- 当前最明确的运行性问题不是主链缺失，而是旧接口仍被现场调用导致 404。
- 先补兼容层，可以在不打断现有调用方的前提下先稳住系统，再继续推进未完成对象。
- 下一步应继续做两件事：
  1. 跑完最小兼容回归脚本并记录结果；
  2. 开始梳理真正未启动对象（`electronics_workshop`、`bsc`、`clean_bench`、`ivc`）的接入顺序与缺口。
