# X1-第二批对象接入施工单-food_workshop

更新时间：2026-04-17 13:54 GMT+8
状态：对象级首轮收口完成 / 后续仅留联调精修

## 当前目标

按“复用已验证骨架、少发明、先打通再精修”的方法，把 `food_workshop`（食品车间）推进到 **对象级首轮可交付状态**。当前该目标已完成：六层主链接入、正文模板第 4 张表真实回归、四等级最小导出与口径同步均已打通。

## 当前判断

- `food_workshop` 属于食品行业独立标准链，不能与 `gmp_workshop` / `electronics_workshop` 混做。
- `static/standards_db.js` 与 `static/standards_ranges.json` 已存在食品四等级与参数范围底座。
- 本机真实模板资源已确认存在：
  - `/Users/fuwuqi/公司资料/检测部/检测报告模板/食品加工/洁净车间检测报告模板.docx`
- 当前已不再是“能否起链”的问题，而是“首轮回归是否稳定、能否正式升为对象级首轮收口”的问题；本轮已完成该确认。

## 本轮已完成动作

1. 接入 `x-model.js`
2. 接入 `x-sample-workspace.js`
3. 接入 `template_rules.py`
4. 接入 `clean_class_semantics.py`
5. 接入 `template_resources.py`
6. 接入 `report_context_builder.py`
7. 生成 `Ⅰ/Ⅱ/Ⅲ/Ⅳ级` 最小 payload
8. 跑首轮解析/模板命中回归
9. 直接解包真实食品模板，按第 4 张表结构核行核格
10. 在 `/Users/fuwuqi/检测报告生成系统_X1/adapters/template_fill.py` 中新增 `food_workshop` 的 `plan.extend([...])` 分支
11. 在 `/Users/fuwuqi/检测报告生成系统_X1/adapters/template_fill.py` 中新增 `food_workshop` 的 `document_xml` 定点写入分支
12. 生成食品四等级首轮正文回归件到：
    - `/Users/fuwuqi/检测报告生成系统_X1/reports_x1/food_regression_round1/`
13. 结合用户新确认业务口径“Ⅰ级测风速，Ⅱ/Ⅲ/Ⅳ级测换气次数”，重跑第二轮四等级正文回归到：
    - `/Users/fuwuqi/检测报告生成系统_X1/reports_x1/food_regression_round2/`
14. 对第二轮四等级回归件进行 XML 级逐行验收

## 当前已确认的正文结果表结构与验收结论

当前食品模板正文关键结果位位于 **第 4 张表（table index = 3）**，第二轮四等级回归已确认稳定：

- `row 3`：空气参数位
  - `Ⅰ级（百级）` 写 **风速 `0.35`**
  - `Ⅱ/Ⅲ/Ⅳ级` 写 **换气次数 `25 / 18 / 12`**
- `row 4`：静压差，稳定写入 `12`
- `row 5`：送风高效过滤器检漏，双结果位稳定写入 `0.008`
- `row 7`：`≥0.5μm` 主行，稳定承载标准值 / 等级 / 最大值 / UCL / 合格结论
- `row 8`：`95%UCL` 辅助行，稳定承载 `0.5μm UCL`
- `row 9`：`≥5μm` 主行，稳定承载标准值 / 最大值
- `row 10`：`95%UCL` 辅助行，稳定承载 `5μm UCL`
- `row 11`：温度，稳定写入 `24`
- `row 12`：相对湿度，稳定写入 `55`
- `row 13`：照度，稳定写入 `300`
- `row 14`：噪声，稳定写入 `58`
- `row 15`：沉降菌，稳定写入 `2`
- `row 16`：浮游菌，稳定写入 `80`

## 当前口径

- `food_workshop` 已从“未起主链对象”推进到 **对象级首轮收口完成**。
- 后续不再按“未开始”或“主链接入中”处理。
- 当前剩余工作仅是后续联调中的轻量显示精修与自然化优化，不构成当前阶段阻塞项。

## 下一步

1. 将 `food_workshop` 的最新状态同步到总台账与对象状态矩阵
2. 后续若遇到真实样本或真人录入差异，再按联调/模板精修项处理
3. 不再把食品对象列为当前未完成主线对象
