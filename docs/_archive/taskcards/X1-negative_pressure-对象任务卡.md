# X1-negative_pressure-对象任务卡.md

更新时间：2026-04-16 11:32 GMT+8
对象：`negative_pressure`
状态：医院洁净部领域主线 / 真实状态核查启动
优先级：P0

---

## 1. 目标

将 `negative_pressure` 做到：
- 纳入医院洁净部领域正式主线
- 明确真实业务边界、模板边界、标准边界
- 确认模型 / 语义 / 规则 / 资源 / 前台 当前真实状态
- 判断它当前处于“未接入 / 半接入 / 可直接起真实 `.filled.docx`”哪一阶段
- 为后续对象级首轮收口建立可靠起点

---

## 2. 计划

1. 优先读取现有施工单与领域台账，确认已有准备程度
2. 核查代码层是否已存在 `negative_pressure` 的：
   - model
   - semantics
   - template rule
   - template resource
   - front workspace 录入
3. 盘点 `reports_x1/` 中是否已有历史导出样本
4. 判断当前主阻塞是在：
   - 未建对象主链
   - 已建规则资源但未真实导出
   - 已导出但未收正文
5. 形成下一步唯一动作

---

## 3. 方法

坚持：
- 真实文件
- 真实代码
- 真实模板资源
- 真实导出样本
- 先核查，再动手
- 每轮只做一个明确动作

禁止凭印象判断。

---

## 4. 结果

### 本轮已完成的真实状态核查

#### 4.1 历史样本已存在
已核到历史真实样本：
- `reports_x1/X1EXPORT_20260415070048.json`
- `reports_x1/X1EXPORT_20260415070048.docx`
- `reports_x1/X1EXPORT_20260415070048.bound.docx`

当前确认：
- 已存在真实 `docx + bound.docx + json`
- 该历史批次未见 `.filled.docx`
- 说明 `negative_pressure` 之前已经进入导出主链前半段，但当时尚未完成正式正文填充收口。

#### 4.2 模型 / 语义 / 规则 / 资源 / 前台 均已有底座
本轮真实核查确认：
- 模型层已存在：`static/x-model.js`
- 前台入口已存在：`static/x-sample-workspace.js`
- 标准参数与范围已存在：
  - `static/standards_db.js`
  - `static/standards_ranges.json`
- 对象语义预览已存在：`adapters/template_fill.py`
- 已有独立施工单：`X1-第二批对象接入施工单-negative_pressure.md`

#### 4.3 历史样本已命中模板规则与模板资源
基于 `X1EXPORT_20260415070048.json` 已确认：
- `template_rule.template_key = hospital/negative_pressure/default`
- `template_resource.template_found = true`
- `template_resource.template_path = /Users/fuwuqi/公司资料/检测部/检测报告模板/医院洁净部/负压病房检测报告模板.docx`
- `clean_class_semantics.level_semantic_key = hospital.negative_pressure.no-clean-class`

说明：
- `negative_pressure` 的规则链和资源链不是空白状态
- “负压病房不以洁净等级为核心”的语义方向也已经进入真实样本

#### 4.4 本轮已补跑第一份真实 `.filled.docx`
本轮基于最小真实 `project payload` 直接调用 `/api/x/submit_export`，已成功产出：
- `X1EXPORT_20260416114636.json`
- `X1EXPORT_20260416114636.docx`
- `X1EXPORT_20260416114636.bound.docx`
- `X1EXPORT_20260416114636.filled.docx`

并已确认返回：
- `success = true`
- `template_ready = true`
- `export_stage = template-bound-ready`
- `filled_docx_path` 已真实存在

#### 4.5 第一轮正文核查结果
基于 `X1EXPORT_20260416114636.filled.docx` 的 `word/document.xml` 已确认命中：
- 项目名称：`负压病房样板项目`
- 委托单位：`X1测试委托单位`
- 项目地址：`呼和浩特市测试地址1号`
- 检测区域：`负压病房一区`
- 项目概述完整句：已命中
- 气象条件：`温度：21.5℃  湿度：36%RH  大气压力：892hPa`
- 判定依据：`GB/T 35428-2017 / WS/T 368-2012`
- 检测结论：已命中“符合判定依据要求/合格”语义
- 参数标签至少已见：`静压差`、`气流流向`、`温度`、`湿度`、`噪声`、`照度`、`细菌浓度`

同时确认：
- `污染区换气次数` 文本未直接命中
- `清洁区换气次数` 文本未直接命中
- `物体表面微生物` 文本未直接命中

这说明：
- `negative_pressure` 已经不是“只有 bound 没有 filled”的状态
- 正文填充链已经启动
- 但深层参数区仍需继续做行级承载位核查，尤其是换气次数双区与表面微生物

#### 4.6 第二轮行级正文核查结果
本轮继续对 `X1EXPORT_20260416114636.filled.docx` 做表格行级扫描，已确认：

- 结果区主表位于 `Table 32`
- 当前已稳定命中的行包括：
  - `换气次数`
  - `排风口风速`
  - `静压差`
  - `送风高效过滤器检漏`
  - `气流流向`
  - `温度`
  - `相对湿度`
  - `平均照度`
  - `噪声`
  - `细菌浓度(沉降法)`

当前行级事实判断：
- `静压差`：正文主表已存在承载位，且单项结论已为 `合格`
- `气流流向`：正文主表已存在承载位，检测结果区已写入 `符合要求`，单项结论为 `合格`
- `换气次数`：正文主表当前只有一个合并后的 `换气次数` 行，标准要求已显示 `10～15`，但**未出现污染区/清洁区双分支文本**
- `细菌浓度(沉降法)`：正文主表已存在承载位，单项结论已为 `合格`
- `物体表面微生物`：当前正文主表未见独立标签或独立承载行

这说明：
- `negative_pressure` 的正文深层参数区不是空的，主表链已经真实存在
- 但当前模板展示形态更像“单主表汇总版”，不是“污染区/清洁区双行细分版”
- `物体表面微生物` 当前要么模板未提供正文承载位，要么尚未被系统补进该模板

#### 4.7 模板变更后的再核查结果（物体表面微生物）
用户已确认：**负压病房检测报告模板已新增“物体表面微生物”行。**
本轮对真实模板 XML 核查确认：
- `负压病房检测报告模板.docx` 当前正文主表已新增：
  - `物体表面微生物`
  - 单位：`cfu/cm²`
  - 标准要求：`≤10`
  - 单项结论位：`合格`
- 行级位置已核到：`Table 32 / Row 42`

随后已同步修改 `adapters/template_fill.py`，为 `negative_pressure` 新增并修正：
- `物体表面微生物` 的对象级定点填充规则
- 最终确认该行的**检测结果位**不在此前误判的列，而应写入该行真实结果单元格

并重新补跑真实导出：
- `X1EXPORT_20260416120836.filled.docx`

当前最新核查确认：
- `物体表面微生物` 新增模板行已真实命中
- 检测结果值 `2` 已成功进入正文该行结果位
- 该项已从“模板新增待接入”推进为“正文结果位已打通”

#### 4.8 当前阻塞点更新
当前阻塞点进一步收敛为：

1. **换气次数双区语义是否有模板真实承载位**
   - 现有正文仍只看到一个 `换气次数` 汇总行
   - 还没看到 `污染区换气次数 / 清洁区换气次数` 双分支展示位
   - 按刘总本轮指示，该问题先挂起，不作为当前继续推进的阻塞项

2. **负压病房剩余工作已转为非换气次数项的对象级收口**
   - `物体表面微生物` 已完成新增模板项接入与结果位打通
   - 后续应继续推进剩余未收项目，但不再纠缠换气次数双区

#### 4.11 当前阶段处理结论（历史记录保留，当前已失效）
按刘总 12:45 最新指示：

> 先挂起，下午调整模板；并建立新的任务卡。

因此当前阶段正式调整为：
- `negative_pressure` 旧模板版本下的 `物体表面微生物` / `细菌浓度(沉降法)` 结果位问题 **暂停继续下钻**；
- 不再继续围绕当前模板版本做 XML 写入硬修；
- 等下午模板调整完成后，再以新模板真实结构为准重新开新一轮收口。

当前保留结论：
- 两行真实结构均已核清：5 个真实 `<w:tc>`；
- 目标结果位均为第 4 个真实 `tc`；
- 当前系统侧已从索引修正推进到底层行级重建，但在旧模板版本下仍未形成最终稳定命中；
- 所以下一轮不应默认继承旧模板结构判断，必须先重新核对调整后的模板。

#### 4.12 当前阻塞点更新（历史阶段结论，当前已被新口径覆盖）
当前阻塞改写为：

1. **旧模板版本链路暂停**
   - `物体表面微生物`：暂停
   - `细菌浓度(沉降法)`：暂停

2. **等待下午模板调整完成**
   - 调整完成后再重新核对承载位与写入逻辑

3. **换气次数双区问题继续挂起**
   - 仍不作为当前动作项

#### 4.13 下一轮唯一动作
下一轮以“模板已调整完成”为前提，先核对新模板真实结构，再恢复 `negative_pressure` 结果位收口。


---

## 5. 验收

### 已完成
- 任务卡建立：已完成
- 已纳入医院洁净部领域主线：已完成
- 历史样本核查：已完成
- 模型/前台/规则/资源底座核查：已完成
- 真实导出阶段判断：已完成
- 第一份真实 `.filled.docx` 试跑：已完成
- 第一轮正文基础命中核查：已完成
- 下一步唯一动作确认：已完成

### 未完成
- 对象级首轮收口：未完成
- 换气次数双区问题：按当前指示暂挂





---

## 6. 总结

`negative_pressure` 这轮已经跨过了“到底接没接进去”的判断阶段。
现在可以明确：

- 它不是空白对象；
- 也不只是停留在 `bound.docx`；
- **第一份真实 `.filled.docx` 已经成功产出。**

这意味着主线已经正式切换为：

> **做负压病房正文深层参数区的逐行核查，确认哪些值已真实落位，哪些仍需对象级定点填充。**

下一轮不再纠缠换气次数双区，而是直接推进 **除换气次数外的剩余收口任务**。
当前已明确：
- `物体表面微生物` 已打通
- 换气次数双区问题按指示先挂起

也就是说，下一步唯一动作已经更新为：

> **围绕 `negative_pressure` 的其余剩余项继续收口，并把任务卡/台账状态同步推进到更接近对象级首轮收口。**


---

_创建时间：2026-04-16 11:32 GMT+8_
