# X1-negative_pressure-模板调整后新任务卡

## 建卡时间
2026-04-16 12:45 GMT+8

## 最新更新时间
2026-04-16 15:23 GMT+8

## 背景
按刘总最新指示：

> 先挂起，下午调整模板。再建立新的任务卡。

因此，旧模板版本下围绕 `物体表面微生物` / `细菌浓度(沉降法)` 结果位的 XML 写入排查，先暂停。
本卡用于承接 **模板调整完成之后** 的下一轮收口，不默认继承旧模板的局部判断，必须重新以新模板真实结构为准。

现在用户已进一步明确：**负压病房模板已经更新，可以回头再试。**
因此本卡状态从“等待恢复”切换为：**已按新模板重新开工，正在基于新模板真实 XML 重建判断。**

---

## 当前已知事实（作为后续起点）
1. 旧模板版本中：
   - `物体表面微生物`
   - `细菌浓度(沉降法)`
   这两行的真实表格结构均已核到是 **5 个真实 `<w:tc>`**。

2. 旧模板版本中，这两行目标结果位判断为：
   - 第 4 个真实 `tc`

3. 系统侧已尝试过：
   - 字段补入 fill plan / replacements
   - 索引修正（0-based）
   - merged-cell 空白结果位补写
   - 行级强制重建目标 cell

4. 但在旧模板版本下，仍未形成稳定命中，因此：
   - 下午模板调整后，**不得默认沿用旧模板的行/列/anchor 结论**；
   - 必须重新基于新模板真实 XML 结构确认承载位。

---

## 新模板首轮重拆结果（2026-04-16 14:36 GMT+8）
已直接核对：
- `/Users/fuwuqi/公司资料/检测部/检测报告模板/医院洁净部/负压病房检测报告模板.docx`

当前已确认：

### 1. `细菌浓度(沉降法)`
- 位置：`Table 3 / Row 12`
- 真实结构：**5 个真实 `<w:tc>`**
- cell 含义：
  1. `细菌浓度(沉降法)`
  2. `cfu/30min·Φ90皿`
  3. 标准位（`≤6`，`gridSpan=2`）
  4. **结果位：当前为空白 cell**
  5. `合格`

### 2. `物体表面微生物`
- 位置：`Table 3 / Row 13`
- 真实结构：**5 个真实 `<w:tc>`**
- cell 含义：
  1. `物体表面微生物`
  2. `cfu/cm²`
  3. 标准位（`≤10`，`gridSpan=2`）
  4. **结果位：当前为空白 cell**
  5. `合格`

### 3. 当前结论
- 新模板下，这两行结构 **仍然是 5-cell 模型**
- 结果位 **仍然是第 4 个真实 `<w:tc>`（0-based index = 3）**
- 但关键变化是：
  - `细菌浓度(沉降法)` 的标准值从旧阶段参考值收敛为 **`≤6`**
  - 说明模板已更新，不能再仅凭旧模板记录行事

## 新模板首轮真实回归结果（2026-04-16 14:47 GMT+8）
已直接使用最小真实 payload 回归导出：
- `export_id = X1EXPORT_20260416144600`
- `filled_docx = /Users/fuwuqi/检测报告生成系统_X1/reports_x1/X1EXPORT_20260416144600.filled.docx`

### 1. payload 已确认带值
- `settling_bacteria.result = 1`
- `surface_bacteria.result = 2`

### 2. 最新 filled 核查结论
- `物体表面微生物`：
  - 仍位于 `Table 3 / Row 13`
  - 仍为 5 个真实 `<w:tc>`
  - **第 4 个真实 tc 仍为空白**
- `细菌浓度(沉降法)`：
  - 本轮尚未证明结果值 `1` 已命中目标结果位
  - 当前仍不能判定为已修通

### 3. 本轮阶段判断
- 新模板结构：**已确认**
- 最小真实回归：**已完成**
- 目标结果位命中：**仍未打通**
- 下一步：**回到 `template_fill.py` 做代码侧定点收口，不再停留在模板核查层**

### 代码侧定点收口推进（2026-04-16 15:23 GMT+8，reset前接力版）
本轮继续沿“不要再猜模板，直接追最终生效链”的方向推进，并已做两件关键动作：

#### 1. 继续升级 `template_fill.py`
已在：
- `/Users/fuwuqi/检测报告生成系统_X1/adapters/template_fill.py`

新增 **negative_pressure 专用硬写函数**：
- `_replace_negative_pressure_result_row(document_xml, row_label, result_text, debug_notes=None)`

其策略不是继续依赖：
- anchor 模糊命中
- table/row 通用函数
- 现有段落替换

而是直接：
1. 扫描整份 `document.xml`
2. 找到包含目标行名的真实 `<w:tr>`
3. 限定该行为 **5 个真实 `<w:tc>`**
4. 锁定第 4 个真实 cell（结果位）
5. 保留 `<w:tcPr>`
6. 重建目标结果位 cell 内容
7. 重组整行并写回

并已将 `negative_pressure` 分支改为优先调用：
- `细菌浓度(沉降法)` → `_replace_negative_pressure_result_row(...)`
- `细菌浓度（沉降法）` → `_replace_negative_pressure_result_row(...)`
- `物体表面微生物` → `_replace_negative_pressure_result_row(...)`

#### 2. 再次做真实回归
已继续生成：
- `X1EXPORT_20260416150835.filled.docx`
- `X1EXPORT_20260416152146.filled.docx`

接口仍然成功：
- `success = true`
- `template_ready = true`

### 当前最新结论（非常关键）
即使已经切到 **negative_pressure 行专用硬写**，最新真实导出里：
- `物体表面微生物`：目标结果位仍空
- `细菌浓度(沉降法)`：仍未拿到结果位命中证据

因此当前问题已进一步收敛为：

> **不是“不会写”，而是“写入没有落到最终生效的 document.xml 上”。**

也就是：
- 业务字段没问题
- payload 带值没问题
- 新模板结构已确认
- 定点写入与专用行级硬写都已经试过
- 但最终 `.filled.docx` 里的目标结果位仍未变化

### 现在最值得追的唯一点
下一轮（reset 后）不要再继续盲改 cell 写法，而要直接核：

1. `_replace_negative_pressure_result_row(...)` 执行后，**内存中的 `document_xml` 是否已经包含 `1/2`**
2. 若中间态已包含，为什么最终打包输出的 `.filled.docx` 没保留
3. 是否存在：
   - 后续替换链覆盖
   - 写到了错误的同名行/错误表
   - debug 文件未按预期产出，说明当前 debug 路径/环境变量链也需一起核

### 当前阶段判断（给 reset 后直接接）
- 这已经不是模板结构问题
- 也不是简单函数替换问题
- 当前主阻塞点已经收敛为：
  **`build_template_filled_docx()` 最终生效链追踪**

---

## 新阶段目标
模板调整完成后，重新完成以下闭环：

1. 核对新模板中以下两行是否存在、文本是否变化：
   - `物体表面微生物`
   - `细菌浓度(沉降法)` / `细菌浓度（沉降法）`

2. 解剖新模板对应行的真实 `<w:tr>/<w:tc>` 结构，确认：
   - 真实 cell 数量
   - 结果位所在 tc
   - 是否存在新的 `gridSpan / vMerge / 空白段落` 差异

3. 再基于新模板，验证 `negative_pressure` 导出链路：
   - payload
   - fill plan
   - replacements
   - docx filled 实际落点

4. 仅在新模板事实确认后，再决定：
   - 继续沿用通用 `_replace_table_row_cells_by_anchor_index()`
   - 或为 `negative_pressure` 新模板单独加专用写法

---

## 暂停项（本卡建立时即生效）
以下问题暂不继续处理：

- 旧模板版本下 `物体表面微生物` 结果位写入
- 旧模板版本下 `细菌浓度(沉降法)` 结果位写入
- `换气次数` 双区问题（继续挂起）

---

## 下一轮恢复时禁止重复踩坑

下午模板调整完成后，恢复本对象时禁止重复以下旧路径：

1. **禁止再把“视觉第几列”当真实写入位**
   - 必须以新模板真实 `<w:tr>/<w:tc>` 结构为准

2. **禁止默认沿用旧模板的 anchor / row_match_index / cell index 判断**
   - 模板一旦调整，旧判断全部视为待复核

3. **禁止在未核对新模板 XML 前先改代码**
   - 先拆模板，再决定是否改 `template_fill.py`

4. **若下午新模板已经显式提供稳定结果位**
   - 优先尝试最小改动
   - 不先上复杂专用写法

## 非暂停项（可继续顺着目标推进）
即使当前模板结果位问题挂起，以下工作仍属于 `negative_pressure` 主线可继续推进项：

- 更新对象状态矩阵
- 固化接力入口
- 收紧下午恢复时的动作顺序
- 明确“旧模板暂停 / 新模板重启”的边界



---

## 2026-04-16 18:08 恢复开工复核（老板视角闭环）

### 1. 目标
把 `negative_pressure` 从“模板调整后暂停态”恢复到“明确唯一阻塞点、可继续推进的主线态”，避免再回到旧模板误判或对象状态混乱。

### 2. 计划
1) 先重新核对总台账、状态矩阵、本任务卡、当日记忆；
2) 再直接重查最新真实模板 `负压病房检测报告模板.docx` 的结果表结构；
3) 把当前真正剩余的问题重新收敛成可执行结论。

### 3. 方法
- 读取：
  - `docs/X1-当前工作总台账.md`
  - `docs/X1-对象接入优先级与状态矩阵.md`
  - 本任务卡
  - `memory/2026-04-16.md`
- 直接解析：
  - `/Users/fuwuqi/公司资料/检测部/检测报告模板/医院洁净部/负压病房检测报告模板.docx`
  - 用 `word/document.xml` 重新核结果表相关行位。

### 4. 结果
本轮重新确认：
1. `negative_pressure` 当前对象状态没有变：
   - 不是未做对象；
   - 不是空壳对象；
   - 已有模型/前台/规则/资源/真实 `.filled.docx` 底座；
   - 当前属于“已打通正文主链，但仍有个别结果位/业务语义待收口”的主线对象。
2. 当前最新版模板中，结果表仍明确只有：
   - `换气次数`（单一结果行）
   - `细菌浓度(沉降法)`
   - `物体表面微生物`
3. 在最新模板 `Table 3` 中再次核到：
   - `换气次数`：`row 3`，只有一个结果位，没有看到 `污染区换气次数` / `清洁区换气次数` 双行；
   - `细菌浓度(沉降法)`：`row 12`，5 个真实 cell，第 4 个真实 cell 为空结果位；
   - `物体表面微生物`：`row 13`，5 个真实 cell，第 4 个真实 cell 为空结果位。

### 5. 验收
本轮不是再做盲改，而是做“恢复前复核”。当前已验明：
- 模板真实结构已重新确认；
- `换气次数` 的未决点依然不是代码 transport 问题，而是**模板只提供单一 carrier，payload 却有污染区/清洁区双值**；
- `细菌浓度(沉降法)` / `物体表面微生物` 的结果位承载行在新模板里依然存在，后续可继续沿命中链收口。

### 6. 总结
`negative_pressure` 恢复开工后，当前唯一正确理解是：
- **阻塞点 A（业务语义）**：换气次数只有单 carrier，如何在 `airchange_polluted=14` 与 `airchange_clean=18` 之间取值，必须按业务/模板口径决定；
- **阻塞点 B（结果位命中）**：`细菌浓度(沉降法)` 与 `物体表面微生物` 的结果位已知存在，但仍需继续追最终生效链，确保值真正写进最终 `.filled.docx`。

当前最合理的下一唯一动作：
> 先做一轮 **负压病房最新模板真实 `.filled.docx` 回归**，确认当前代码状态下，这 3 个点（换气次数 / 细菌浓度(沉降法) / 物体表面微生物）在最终产物中的真实落位结果，再决定是改代码还是提业务口径问题。


### 2026-04-16 18:12 最新模板真实 `.filled.docx` 回归结果
已直接基于：
- `/Users/fuwuqi/检测报告生成系统_X1/temp_x1/negative_pressure_project_payload.json`
- 当前模板资源 `/Users/fuwuqi/公司资料/检测部/检测报告模板/医院洁净部/负压病房检测报告模板.docx`
重新生成：
- `/Users/fuwuqi/检测报告生成系统_X1/reports_x1/NEGATIVE_PRESSURE_RECOVERY_VERIFY.filled.docx`

#### 最新结果表核查（`Table 3`）
- `row 3`：`换气次数 | 次/h | 10～15 | [空] | 合格`
- `row 12`：`细菌浓度(沉降法) | cfu/30min·Φ90皿 | ≤6 | 1 | 合格`
- `row 13`：`物体表面微生物 | cfu/cm² | ≤10 | 2 | 合格`

#### 新阶段判断
1. 之前 `细菌浓度(沉降法)` / `物体表面微生物` 未命中的主阻塞，**在当前模板版本下已打通**；
2. 当前 `negative_pressure` 剩余唯一未决点，已收敛为：
   - 模板正文只有单一 `换气次数` carrier；
   - payload 中却存在 `污染区换气次数=14`、`清洁区换气次数=18` 两个业务值；
   - 当前 `.filled.docx` 中 `换气次数` 结果位仍为空，说明这不是模板没有 carrier，而是系统尚未决定该把哪个值写进去。

#### 当前唯一下一步
> 向刘总确认：负压病房正文中的单一 `换气次数` 行，应当取 **污染区换气次数**、**清洁区换气次数**、还是按某种规则合成/改模板。确认后即可完成该对象最后收口。
