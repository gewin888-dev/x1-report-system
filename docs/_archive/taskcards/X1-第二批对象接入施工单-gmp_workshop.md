# X1-第二批对象接入施工单-gmp_workshop

更新时间：2026-04-20 15:08 GMT+8
对象：`gmp_workshop`
领域：制药
优先级：P1
状态：历史施工单（已失效的起步口径） / 当前对象已完成对象级首轮收口

---

## 0. 当前口径说明（新增）

本文件形成于 `gmp_workshop` 尚处早期核查 / 起步推进阶段，因此标题虽保留“第二批对象接入施工单”，但其“未接入主链 / 第二批关键复杂对象首位”的旧状态口径已经失效。

当前最新口径应以以下文档为准：
1. `docs/X1-当前工作总台账.md`
2. `docs/X1-对象接入优先级与状态矩阵.md`
3. `docs/X1-业务领域-检测类型覆盖台账.md`

当前对 `gmp_workshop` 的正确判断是：
- 已进入 X1 主链
- 已完成对象级首轮收口
- 不再按“未接入主链对象”或“第二批待起步对象”管理
- 后续如再处理，仅按版本升级、联调精修或回归复核处理

保留本文件的目的，是保存它在“从早期旧判断推进到真实状态重判”过程中的核查价值，而不是继续把它当当前执行状态。

---

## 1. 当前目标

将 `gmp_workshop` 从“未接入主链”推进到：
- 明确业务边界
- 明确等级语义边界
- 明确模板规则边界
- 明确资源与前台接入边界
- 为后续最小真实 `.filled.docx` 验收建立施工起点

---

## 2. 业务边界

`gmp_workshop` 属于制药领域复杂空间对象，不能与以下对象混做：
- `food_workshop`
- `electronics_workshop`
- `veterinary_gmp_workshop`

其核心特征应围绕：
- A / B / C / D 级洁净等级
- 静态 / 动态语义
- 药品生产相关环境参数
- 制药洁净车间模板资源匹配

---

## 3. 当前计划

1. 先核制药领域现有对象基础，确认可复用项
2. 核查代码层是否已有 `gmp_workshop` 残留语义、等级、模板线索
3. 梳理 `gmp_workshop` 应具备的最小参数主链
4. 确认是否存在可直接映射的真实模板资源
5. 形成第一轮施工结论：
   - 未建对象骨架
   - 还是已有碎片可接

---

## 4. 方法

坚持：
- 真实代码
- 真实模板
- 真实资源
- 真实标准语义
- 先核查，再动手
- 每轮只做一个明确动作

---

## 5. 当前应重点核查项

### 模型
- 是否已有 `gmp_workshop` type_id
- 是否已有 GMP 参数结构或等级字段
- **本轮核查结果：已存在**
  - `static/x-model.js` 已注册 `gmp_workshop`
  - 默认 `clean_class = C级`
  - 已有 `context.gmp_grade` 与 `gmp_grade_options`
  - 已有 basis / judgement / summary 默认值

### 语义
- 是否已有 A/B/C/D 等级语义
- 是否已有静态/动态语义入口
- **本轮核查结果：已存在**
  - `clean_class_semantics.py` 已有 `pharma.gmp_workshop` 分支
  - 标准已绑定：`GB 50457-2019 + GMP 2010`
  - 语义键：`pharma.gmp_workshop.grade.<grade>`

### 规则
- 是否已有模板规则映射
- 是否已有制药洁净车间模板键
- **本轮核查结果：已存在**
  - `template_rules.py` 已有 `pharma.gmp_workshop`
  - 已支持 A/B/C/D 四档模板 key：
    - `pharma/gmp_workshop/grade/a`
    - `pharma/gmp_workshop/grade/b`
    - `pharma/gmp_workshop/grade/c`
    - `pharma/gmp_workshop/grade/d`

### 资源
- 是否已有真实模板文件可命中
- 模板是否属于药厂/GMP 车间，而不是食品/电子/兽药
- **本轮核查结果：已存在且已确认真实路径**
  - `template_resources.py` 已注册 A/B/C/D 四档资源
  - 当前统一映射到：
    - `/Users/fuwuqi/公司资料/检测部/检测报告模板/制药工业/GMP车间检测报告模板.docx`
  - 已确认制药工业目录真实存在，不是食品/电子模板

### 前台
- 是否已有录入入口
- 是否已有与 GMP 相关的房间类型选项
- **本轮核查结果：已存在**
  - `static/x-sample-workspace.js` 已有 `gmp_workshop` 按钮入口
  - 已有 `GMP等级` 录入组件
  - 已有 `A级 / B级 / C级 / D级` 下拉选项

---

## 6. 第一轮真实结论

本轮已确认：

`gmp_workshop` **不是未建对象，也不是纯空白对象**，而是已经具备以下基础：
- 模型已接
- 语义已接
- 规则已接
- 资源已接
- 前台已接
- 真实模板文件已存在
- 历史上还存在与 `gmp_workshop` 相关的导出样本痕迹

因此当前正确判断应从：
- “未接入主链”

改为：
- **已具备对象骨架与模板资源基础，但尚未进入对象级真实验收收口**

---

## 7. 当前验收

第一轮施工验收结果：
- [x] 是否已有对象骨架：明确，**有**
- [x] 是否已有语义碎片：明确，**不是碎片而是已成型语义分支**
- [x] 是否已有模板规则/资源：明确，**有且模板路径已真实确认**
- [x] 下一步唯一动作：明确

---

## 8. 第二轮真实样本阶段核查结果

本轮继续按“每轮一个明确动作”推进，直接核查 `gmp_workshop` 历史样本当前走到哪一阶段。

### 8.1 样本存在情况
已核到与 `gmp_workshop` 相关的历史样本批次至少包括：
- `X1EXPORT_20260414230516`
- `X1EXPORT_20260415061409`
- `X1EXPORT_20260415073601`
- `X1EXPORT_20260415074327`
- `X1EXPORT_20260415084859`

其中阶段分布已确认：
- 存在 `json + docx + bound.docx` 批次
- 也已存在 `json + docx + bound.docx + filled.docx` 批次

### 8.2 当前阶段判断
虽然部分历史 `json` 文件内容结构较旧，已无法直接从 `project_snapshot/template_rule/template_resource` 字段中稳定回读对象信息，
但结合：
- 历史样本命名批次
- `filled.docx` 真实存在
- 代码层 `gmp_workshop` 规则/资源/语义/前台已成型

当前已经可以确认：

`gmp_workshop` **不只是 template-bound-ready，历史上已经走到过 `.filled.docx` 阶段**。

这意味着当前主缺口进一步收敛为：
- 不是“有没有进入导出后半段”
- 而是 **历史 `.filled.docx` 的正文是否真的达到了对象级可验收状态**
- 以及当前链路是否还能稳定复现到新的最小真实 payload 上

### 8.3 当前结论更新
因此，`gmp_workshop` 当前正确定位应改为：

- 已具备模型/语义/规则/资源/前台基础
- 历史上已走通过 `.filled.docx` 阶段
- 但**尚未完成对象级真实验收收口**
- 下一步应转向：
  1. 核查历史 `.filled.docx` 的正文命中情况
  2. 再决定是否补最小真实 payload 做新一轮回归试跑

---

## 9. 第三轮历史 `.filled.docx` 正文核查结果

本轮继续按唯一动作推进，直接解剖 `gmp_workshop` 历史 `.filled.docx` 正文内容，确认当前到底命中了哪些对象级内容。

### 9.1 已确认命中的基础正文区
对以下样本：
- `X1EXPORT_20260415074327.filled.docx`
- `X1EXPORT_20260415084859.filled.docx`

已确认正文中稳定命中：
- `项目名称`
- `委托单位`
- `项目地址`
- `联系方式`
- `检测区域`
- `项目概述`
- `受检区域`
- `检测状态`
- `气象条件`
- `判定依据`
- `检测结论`

并已确认：
- `检测结论` 句式已落正文：
  - `本次检测的所测参数，均符合判定依据中的相应规定。`
- `判定依据` 已命中：
  - `GB 50457-2019`

### 9.2 已确认命中的主结果区关键词
同批 `.filled.docx` 中已命中以下对象级参数关键词：
- `温度`
- `相对湿度`
- `静压差`
- `换气次数`
- `照度`
- `噪声`
- `洁净度`
- `浮游菌`
- `沉降菌`

这说明：
- `gmp_workshop` 不只是基础公共字段落正文；
- 历史 `.filled.docx` 中主结果区参数链也已经真实存在；
- 当前对象级收口不是“从无到有”，而是要进一步核对这些结果区是否已经达到可验收状态。

### 9.3 当前结论再收敛
截至本轮，可以把 `gmp_workshop` 当前阶段明确为：

- 基础公共字段：**已命中正文**
- 主结果区参数标签：**已进入正文**
- 历史 `.filled.docx`：**真实存在**
- 当前未完成项：
  - 这些主结果区的结果值 / 结论位 / 表格落点是否已经达到对象级可验收状态
  - 是否需要针对 GMP 模板补“结果位定点收口”

---

## 12. 主结果表已推进到“结果位验收前夜”

本轮继续推进后，`gmp_workshop` 主结果表的关键单行参数已确认存在：
- 单位位
- 标准位
- 单项结论位

已确认项目：
- `换气次数`：`次/h` / `≥10` / `合格`
- `静压差`：`Pa` / `≤-5(对缓冲间)` / `合格`
- `温度`：`℃` / `18～26` / `合格`
- `相对湿度`：`%` / `45～65` / `合格`
- `平均照度`：`lx` / `≥200` / `合格`
- `噪声`：`dB(A)` / `≤60` / `合格`
- `沉降菌（平均菌落数）`：`cfu/30min·Φ90皿` / `≤10` / `合格`
- `浮游菌（平均浓度）`：`个/m³` / `≤100` / `合格`

同时已确认：
- `洁净度` 已进入结果链，但属于复合结构块，不应按普通单行参数验收。

这意味着当前 `gmp_workshop` 的剩余不确定点，已进一步压缩为：
- 单行参数的结果位是否完整
- `洁净度` 复合块是否结构稳定

---

## 13. 下一轮唯一动作

> 输出 `gmp_workshop` 的“完整态 / 缺结果位 / 复合结构待单列验收”清单，并据此判断是否可进入首轮对象级收口。

---

## 14. 第四轮复核结果（2026-04-16 20:00-20:01）

### 14.1 本轮目标
把 `gmp_workshop` 从“还在猜结果位是否存在”推进到：
- 直接用历史 `.filled.docx` 实拆结果输出最终对照表
- 明确哪些是“完整态”
- 明确哪些是“缺结果位”
- 明确 `洁净度` 是否继续作为复合结构项单列验收

### 14.2 方法
本轮直接实拆两份历史 `.filled.docx`：
- `X1EXPORT_20260415074327.filled.docx`（C级）
- `X1EXPORT_20260415084859.filled.docx`（D级）

重点核查主结果表真实 `table_index / row_index / cells`，不再停留在关键词级判断。

### 14.3 结果
已确认：
- 主结果表位于 `table_index = 3`
- `换气次数 / 静压差 / 温度 / 相对湿度 / 平均照度 / 噪声 / 沉降菌 / 浮游菌` 在 C级 / D级 两份样本中的结果表结构完全一致；
- 这些参数行当前都已具备：
  - 参数名
  - 单位位
  - 标准位
  - 结论位（`合格`）
- 但第 4 个真实 cell（结果位）当前全部为空，说明：
  - 历史链路虽已形成主结果表，
  - 但**结果值并未真正进入最终正文结果位**。

### 14.4 当前分类结论
#### A. 缺结果位项
- `换气次数`
- `静压差`
- `温度`
- `相对湿度`
- `平均照度`
- `噪声`
- `沉降菌（平均菌落数）`
- `浮游菌（平均浓度）`

#### B. 复合结构待单列验收项
- `洁净度级别（悬浮粒子浓度）`
  - 当前已确认其为复合结果块；
  - C级 / D级 两份样本均保留该结构；
  - 不应按普通单行参数处理。

#### C. 当前尚不能列入完整态的原因
虽然这些行已具备单位位 / 标准位 / 结论位，但由于结果位为空，因此当前不能判为“完整态”或“对象级首轮收口完成”。

### 14.5 新增业务要求（2026-04-16 20:12）
刘总已明确：
> `gmp_workshop` **A / B / C / D 四个等级都要做**，不能只拿已有 C / D 两个历史样本当作收口范围。

因此当前对象级收口标准升级为：
1. 不能只验证 C / D；
2. 必须把 A / B / C / D 四个等级都纳入真实验收范围；
3. 若 A / B 当前缺历史样本，则后续必须补：
   - 最小真实 payload
   - 对应模板命中
   - `.filled.docx` 结果位回归

### 14.6 验收
本轮已完成：
- [x] 输出单行参数最终对照表
- [x] 明确 C级 / D级 结构一致性
- [x] 明确结果位当前真实状态为“空”
- [x] 明确 `洁净度` 应单列为复合结构项
- [x] 明确后续 `gmp_workshop` 收口范围必须覆盖 A / B / C / D 四个等级

### 14.7 下一步
> 下一步不再继续讨论 `gmp_workshop` 是否“已接入”，而是直接进入：
> 1. 先检查 A / B / C / D 四个等级的模板资源与导出路径是否齐备；
> 2. 以四等级为验收范围，逐步把 8 个单行参数结果值打入主结果表第 4 个真实 cell；
> 3. 单列验收 `洁净度` 复合结构块。

---

## 15. 第五轮复核结果（2026-04-16 20:48）

### 15.1 目标
按刘总“ABCD 各等级都要做”的要求，先确认四等级当前在规则/资源/路径层面是否已经具备推进条件，避免后面真正回归时才发现 A / B 根本没有路由或资源。

### 15.2 方法
直接核查：
- `template_rules.py`
- `template_resources.py`
- 实际调用 `resolve_template_rule()` + `resolve_template_resource()`，对 A / B / C / D 四等级逐一解析。

### 15.3 结果
已确认：
- A / B / C / D 四个等级当前全部都有独立 `template_key`；
- 四个等级全部都有独立 `template_name`；
- 四个等级统一走 `report_context_mode = pharma-gmp-grade`；
- 四个等级当前全部命中同一个真实模板文件：
  `/Users/fuwuqi/公司资料/检测部/检测报告模板/制药工业/GMP车间检测报告模板.docx`
- 这说明当前不是 A / B 缺规则、缺资源或缺模板路径，而是**四等级都已具备继续回归推进的底座**。

### 15.4 当前结论
`gmp_workshop` 当前四等级推进条件已明确：
- 规则层：齐备；
- 资源层：齐备；
- 路由层：齐备；
- 剩余主缺口已进一步收敛为：
  1. A / B 是否已有历史样本；
  2. 若无历史样本，是否需补最小真实 payload；
  3. 四等级导出链是否都能把 8 个单行参数结果值打进主结果表结果位；
  4. `洁净度` 复合结构块是否在四等级下保持稳定。

### 15.5 验收
本轮已完成：
- [x] 核 A / B / C / D 四等级路由齐备性
- [x] 核 A / B / C / D 四等级模板资源齐备性
- [x] 实测四等级全部可命中真实模板路径

### 15.6 下一步
> 直接进入样本/payload 层：
> 先查 A / B 是否已有历史导出样本；若没有，则补 A / B 最小真实 payload，随后进入四等级结果位写入收口。

---

## 16. 第六轮复核结果（2026-04-16 21:20）

### 16.1 目标
把 `gmp_workshop` 的四等级要求真正落到样本层，确认当前是否已经存在 A / B 历史样本，以及现有 C / D 历史样本能否直接作为当前真实回归输入。

### 16.2 方法
直接扫描 `/Users/fuwuqi/检测报告生成系统_X1/reports_x1` 下全部 `X1EXPORT_*.json`，筛选 `type_id = gmp_workshop`，并核对：
- 等级
- `.docx / .bound.docx / .filled.docx` 是否存在
- `export_payload.room.params` 是否有内容
- `template_rule / template_resource` 是否已落盘

### 16.3 结果
已确认：
- 当前历史样本只有 C级 / D级；
- 当前未发现 A级历史样本；
- 当前未发现 B级历史样本；
- 现有两份已实拆的 C / D `.filled.docx` 对应 `.json` 中：
  - `template_rule = {}`
  - `template_resource = {}`
  - `export_payload.room.params = []`
- 这说明当前 C / D 历史样本只能证明“曾走到 `.filled.docx` 阶段”，但**不能直接作为当前完整真实回归输入**。

### 16.4 当前结论
`gmp_workshop` 当前推进状态已经可明确分层：
1. 四等级规则/资源/路由层：齐备；
2. 历史样本层：仅有 C / D，缺 A / B；
3. 结果链层：C / D 历史 `.filled.docx` 已证实结果位为空；
4. payload 层：现有历史 `.json` 的 `params/template_rule/template_resource` 都不完整，必须重建真实回归输入。

### 16.5 验收
本轮已完成：
- [x] 扫描全部 `gmp_workshop` 历史导出样本
- [x] 确认 A / B 历史样本缺失
- [x] 确认现有 C / D 历史 `.json` 不能直接作为当前完整回归输入

### 16.6 下一步
> 当前下一步已经收敛为：
> 1. 补 A / B 最小真实 payload；
> 2. 同时为 C / D 重建带完整 `params / template_rule / template_resource` 的真实回归 payload；
> 3. 以四等级口径统一推进结果位写入收口。

---

## 17. 第七轮推进结果（2026-04-16 21:32）

### 17.1 目标
把“补 A / B payload、重建 C / D payload”从计划项变成真实产物，直接形成四等级统一可用的最小真实回归输入底座。

### 17.2 方法
基于以下已确认结构：
- `template_rules.py`
- `template_resources.py`
- `report_context_builder.py`
- `static/x-model.js` 中 `gmp_workshop` 的当前语义结构

程序化构造四等级最小真实回归 payload，统一补齐：
- `project`
- `room`
- `template_rule`
- `template_resource`
- `report_context`
- 8 个单行参数 `params`

并正式落盘到：
`/Users/fuwuqi/检测报告生成系统_X1/temp_x1/gmp_payloads/`

### 17.3 结果
已生成 4 份 payload：
- `gmp_A级_minimal_export_payload.json`
- `gmp_B级_minimal_export_payload.json`
- `gmp_C级_minimal_export_payload.json`
- `gmp_D级_minimal_export_payload.json`

并已确认四份 payload 都具备：
- 对应等级 `template_key`
- `template_found = true`
- `param_count = 8`

### 17.4 当前结论
`gmp_workshop` 当前已经推进到：
- 四等级最小真实回归 payload 已具备；
- A / B 不再受“没有历史样本”限制；
- C / D 不再依赖历史残缺 `.json`；
- 下一步可直接进入：**用这 4 份 payload 跑真实 `.filled.docx`，检查 8 个单行参数结果位是否命中主结果表第 4 个真实 cell。**

### 17.5 验收
本轮已完成：
- [x] 构造 A / B / C / D 四等级最小真实回归 payload
- [x] 落盘四份 payload 文件
- [x] 确认四份 payload 均能命中真实模板资源

### 17.6 下一步
> 直接用四份 payload 跑 A / B / C / D 的 `.filled.docx` 回归，并开始结果位写入验收。

---

## 18. 第八轮实回归结果（2026-04-16 21:59）

### 18.1 目标
直接干结果位，不再停在计划层：
- 用 A / B / C / D 四份 payload 实跑 `.filled.docx`
- 确认真实结果位状态
- 如果结果位仍空，立即修 `template_fill.py`

### 18.2 方法
本轮已执行：
1. 先实跑四等级 `.filled.docx`；
2. 确认四等级结果位仍为空；
3. 立即修 `adapters/template_fill.py`，把 `gmp_workshop` 的 fill-plan 和正文结果表写入链补齐；
4. `py_compile` 通过后再次实跑四等级回归；
5. 逐份拆解最终 `.filled.docx`，核主结果表 `table_index = 3` 对应结果位。

### 18.3 结果
已确认：
- A / B / C / D 四等级 8 个单行参数结果位已全部打通；
- 结果值已真实进入主结果表第 4 个真实 cell。

四等级实测结果如下：
- A级：换气次数 `120` / 静压差 `15` / 温度 `22.0` / 相对湿度 `55` / 平均照度 `320` / 噪声 `58` / 沉降菌 `0` / 浮游菌 `0`
- B级：换气次数 `60` / 静压差 `12` / 温度 `21.8` / 相对湿度 `54` / 平均照度 `300` / 噪声 `57` / 沉降菌 `2` / 浮游菌 `8`
- C级：换气次数 `18` / 静压差 `10` / 温度 `22.1` / 相对湿度 `52` / 平均照度 `280` / 噪声 `55` / 沉降菌 `5` / 浮游菌 `80`
- D级：换气次数 `15` / 静压差 `8` / 温度 `22.3` / 相对湿度 `50` / 平均照度 `260` / 噪声 `54` / 沉降菌 `8` / 浮游菌 `95`

当前实测回归文件：
- `reports_x1/gmp_regression_after_fix/GMP_A级_REGRESSION.filled.docx`
- `reports_x1/gmp_regression_after_fix/GMP_B级_REGRESSION.filled.docx`
- `reports_x1/gmp_regression_after_fix/GMP_C级_REGRESSION.filled.docx`
- `reports_x1/gmp_regression_after_fix/GMP_D级_REGRESSION.filled.docx`

### 18.4 当前结论
`gmp_workshop` 先前最核心的阻塞点——
**“ABCD 四等级 8 个单行参数结果位为空”**——已经被真实回归解除。

当前剩余问题已收敛为：
1. `洁净度级别（悬浮粒子浓度）` 复合结构块四等级验收；
2. 在此基础上判断是否进入对象级首轮收口。

### 18.5 验收
本轮已完成：
- [x] 四等级真实 `.filled.docx` 回归
- [x] `template_fill.py` 实修
- [x] 四等级 8 个单行参数结果位实测命中

### 18.6 下一步
> 直接转入 `洁净度` 复合结构块四等级验收；若稳定，则推进 `gmp_workshop` 对象级首轮收口判断。

---

## 19. 第九轮实回归结果（2026-04-16 22:11）

### 19.1 目标
继续直推 `gmp_workshop`，不转移话题：
- 做 `洁净度级别（悬浮粒子浓度）` 复合结构块四等级验收；
- 看它是否已经足以支持对象级首轮收口。

### 19.2 方法
本轮已直接执行：
1. 拆解四份最新 `GMP_A/B/C/D_REGRESSION.filled.docx`；
2. 精查主结果表 `table_index = 3` 的 `row 7 ~ row 11`；
3. 发现复合块未完全写入后，继续追输入层；
4. 确认最小真实 payload 缺 `particle` 复合参数；
5. 立即把四份 payload 全部补上 `particle`；
6. 修改 `template_fill.py` 让 `gmp_workshop` 显式输出：
   - `洁净度级别`
   - `洁净度`
   - `悬浮粒子数/m³`
   - `≥0.5μm`
   - `≥5μm`
   - `0.5μmUCL`
   - `5μmUCL`
7. 再次编译并重跑四等级 `.filled.docx`。

### 19.3 结果
当前已确认：
- 四等级标题行 `row 7` 已正确保留 A/B/C/D；
- 四等级 `≥0.5μm` 行 `row 8` 已真实写入：
  - 洁净度级别
  - 最大值
  - UCL

实测结果：
- A级 → `GMP静态A级 / 3200 / 3000`
- B级 → `GMP静态B级 / 3500 / 3300`
- C级 → `GMP静态C级 / 320000 / 300000`
- D级 → `GMP静态D级 / 3200000 / 3000000`

### 19.4 当前结论
`gmp_workshop` 的 `洁净度` 复合结构块已经不是“未接入”，而是：
- 上半链已接通；
- 已完成首轮四等级实回归；
- 但还没有做到对象级收口要求的“完全稳定”。

当前剩余未闭环点：
1. `≥5μm` 对应行仍未写入；
2. `95%UCL` 下层行仍未单独落位；
3. 标准位当前仍是模板原生统一值，未完成 A/B/C/D 等级化精修；
4. 本轮已进一步确认：当前阻塞点不在 payload，也不在 fill-plan，而在 **GMP 模板洁净度复合块的真实 cell 结构索引仍需单独钉死**。

### 19.5 判断
所以现在不能直接把 `gmp_workshop` 宣布为“已首轮收口”。
它的真实状态应更新为：

> 单行参数结果位已全部打通；  
> 洁净度复合结构块已完成首轮四等级验收，但当前正处于 **真实表格索引精修阶段**。

### 19.6 下一步
> 下一步直接拆 GMP 模板 `row 8~11` 的真实 cell 结构（含合并/空单元格关系），把 `≥5μm` 与 `95%UCL` 的落点索引钉死，然后再跑四等级实回归；完成后再做对象级首轮收口判断。

---

## 20. 第十轮实回归结果（2026-04-16 23:20）

### 20.1 目标
不接受“知道该查什么”就停，继续把 `gmp_workshop` 的复合块从“索引待查”推进到“真实模板结构已拆清、当前真实写入值已确认”。

### 20.2 方法
本轮继续直接做真动作：
1. 在四份最小真实 payload 中补入 `particle` 后，再次实跑 A / B / C / D `.filled.docx`；
2. 继续修改 `adapters/template_fill.py`，把 GMP 复合块写入推进到：
   - `row 8`：等级位 / `≥0.5μm` 最大值 / `0.5μmUCL`
   - `row 10`：`≥5μm` 最大值 / `5μmUCL`
3. 直接拆解真实模板 `/Users/fuwuqi/公司资料/检测部/检测报告模板/制药工业/GMP车间检测报告模板.docx` 的 `table_index = 3 / row 7~11`，逐行核对 `<w:tc>` 数量、`gridSpan`、`vMerge` 与文本承载位。

### 20.3 结果
已确认当前四等级真实写入结果如下：
- A级 → `GMP静态A级 / 3200 / 3000 / 20 / 18`
- B级 → `GMP静态B级 / 3500 / 3300 / 25 / 22`
- C级 → `GMP静态C级 / 320000 / 300000 / 2000 / 1800`
- D级 → `GMP静态D级 / 3200000 / 3000000 / 20000 / 18000`

并已把真实模板结构拆清到以下结论：
- `row 7`：7 个真实 `<w:tc>`，是复合块表头行；
- `row 8`：8 个真实 `<w:tc>`，为 `≥0.5μm` 主数值行；
- `row 9`：8 个真实 `<w:tc>`，为 `95%UCL` 说明行；
- `row 10`：8 个真实 `<w:tc>`，为 `≥5μm` 主数值行；
- `row 11`：8 个真实 `<w:tc>`，为 `95%UCL` 说明行；
- 因此当前问题已不应再表述成“复合块没通”或“还缺 payload”，而应明确为：
  **GMP 复合块已经通，剩余是 merged-cell 业务语义与最终对象级验收口径的精修。**

### 20.4 当前结论
到这一步，`gmp_workshop` 当前真实状态应更新为：
- 8 个单行参数结果位已全部打通；
- `particle` 复合 carrier 已接通；
- `洁净度` 复合结构块主要数值位已进入正文；
- 当前处于 **真实 merged-cell 语义精修 + 最终对象级收口判断前夜**，而不是“主链未通”状态。

### 20.5 验收
本轮已完成：
- [x] 四等级 `particle` 驱动实回归
- [x] `≥5μm / UCL` 主数值位实写确认
- [x] 真实模板 `row 7~11` cell 结构拆解完成

### 20.6 下一步
> 下一步唯一动作：围绕 `row 10 / row 11` 做最后一次定点修补与实回归，把 `5μm 最大值 / UCL` 的真实可见承载位打通，并决定是否同步做标准位等级化；完成后即可做 `gmp_workshop` 的对象级首轮收口判断。

---

## 21. 第十一轮实推进结果（2026-04-16 23:34）

### 21.1 目标
不再停留在“要出最终对照表”的口头层，而是直接把 GMP 模板 `row 8~11` 的最终验收对照表做出来，用真实模板 + 真实回归件对照，给 `gmp_workshop` 一个可收口的明确结论。

### 21.2 方法
本轮直接执行：
1. 解压真实模板 `GMP车间检测报告模板.docx`；
2. 解压最新回归件 `reports_x1/gmp_regression_after_fix/GMP_D级_REGRESSION.filled.docx`；
3. 针对 `table_index = 3 / row 7~11`，逐格比对：
   - cell 编号
   - `gridSpan / vMerge`
   - 模板原始文本
   - 回归件当前写入文本
4. 输出“真实 cell 编号 → 业务含义 → 当前写入值 → 是否达到验收口径”的最终对照表结论。

### 21.3 结果
本轮最终对照后已确认：
- `row 7`：对象等级位 `D级` 与判定位 `合格` 已稳定命中；
- `row 8`：
  - `GMP静态D级` 已稳定命中；
  - `3200000`（0.5μm 最大值）已稳定命中；
  - `3000000`（0.5μm UCL）已稳定命中；
- `row 9`：`95%UCL` 为模板固定说明行；当前发现 `cell 5` 被重复写入第二个 `95%UCL`，属于表现层精修项；
- `row 10`：
  - `20000`（5μm 最大值）已成功进入可见结果位；
  - `18000`（5μm UCL）已成功进入对应 merged-cell 承载区；
  - 但 `cell 3` 同时出现 `20000`，说明纵向承接位被一起写脏，属于表现层精修项；
- `row 11`：`95%UCL` 说明行存在，但 `cell 5` 也被重复写入第二个 `95%UCL`，同样属于表现层精修项。

### 21.4 当前结论
这一步之后，`gmp_workshop` 的状态已经可以最终收敛为：
- 主结果链已闭环；
- `洁净度` 复合结构块核心业务数值（0.5μm / 5μm / UCL）均已进入正文；
- 当前剩余的已经不是“结果未通”，而是 **merged-cell 表现层清洁 + 标准位等级化** 两类轻量尾项。

也就是说，当前 `gmp_workshop` **已经达到对象级首轮收口可判定门槛**，只是还不适合马上宣告“完全无尾项”。

### 21.5 验收
本轮已完成：
- [x] `row 10 / row 11` 定点修补
- [x] 最新 `.filled.docx` 实回归
- [x] 5μm 最大值 / UCL 主结果链打通
- [x] 将剩余问题缩窄为表现层精修项

### 21.6 下一步
> 下一步只做一次“表现层收边”：清理 `row 10 / cell 3` 与 `row 9 / row 11` 的重复 `95%UCL` 写入，并决定是否同步做 `≥0.5μm / ≥5μm` 标准位等级化；完成后即可把 `gmp_workshop` 升级为对象级首轮收口完成。
